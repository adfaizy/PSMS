# Vite `base` (leading `/`; use trailing `/ for a subpath, e.g. /gsms/).
# The bundled Docker nginx is laid out for `/`. Subpaths need your own server config.
VITE_BASE=/

# Host port when using docker-compose (maps to container 80).
PORT=8080
